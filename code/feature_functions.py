"""
bot_inflated
organic
"""
import re
def f1(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["gg ggy"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i] == j[0].lower()):	
					return 1
	return 0

def f2(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["RGARMY.HATEPAGE"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f3(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Robert Oeser"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f4(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["500 Subscribers without A video Challenge?"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f5(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Top Hater"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f6(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Hawkeyefan95IA"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f7(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["The Dominique Harris Channel"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f8(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["brave nimz"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f9(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["thaaffguy"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f10(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Nadine Bowling"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f11(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Bozica Ristova"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f12(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["akthercho"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f13(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["velimirka zusamen"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f14(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["kutti kavi"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f15(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["theCVman"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f16(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Matty Marz"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f17(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["PXRRIS DANIEL"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f18(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Brad Cowen"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f19(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Connor Lexington"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f20(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Ravi Bedia"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f21(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["IZMCreditServices.net"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f22(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Melanee Davis"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f23(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["Krassi Popov"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f24(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["castaway140"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i] == j[0]):	
					return 1
	return 0

def f25(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["John David"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f26(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["MistafloX MistafloX"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f27(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["NewEraCap50"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f28(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["MrLifeChat"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f29(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			s = ["ChuckTaylormfc05"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() == j[0].lower()):	
					return 1
	return 0

def f30(sentence, tag):
	for comment in sentence:
		if(tag == "organic"):
			s = ["ChuckTaylormfc05"]
			j = comment.split("\t")
			for i in range(len(s)):
				if(s[i].lower() != j[0].lower()):	
					return 1
	return 0

def f31(sentence, tag):
	for comment in sentence:
		if(tag == "bot_inflated"):
			pattern = "https://"
			j = comment.split("\t")
			if(re.search(pattern, j[1])):	
				return 1
	return 0